# Copy to functions/.env for local emulator use or configure these in your Firebase project.
# At least one real provider key is recommended. If none are set, the backend returns a setup message.
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
MISTRAL_API_KEY=
